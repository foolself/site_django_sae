{% extends 'base.html' %}
{% load pagination_tags %}
{% block content %}
{% autopaginate article_list 6 %}
	{% for article in article_list %}
		<hr class="featurette-divider">
		<div class="row featurette">
        	<div class="col-md-8 col-md-push-4">
				<h2 class="featurette-heading"><a href="{% url 'blog.views.article_detail' pk=article.pk %}">{{ article.title }}</a></h2>
				<p>published: {{ article.published_date| date:'Y-m-d' }}</p>
				<p class="lead">{{ article.text | striptags | truncatechars:100 }}</p>
				<div>
					<span class="glyphicon glyphicon-tags">
						{% for tag in article.tag.all %}
            			<a href="?tag={{ tag.name }}" rel="tag">{{ tag.name }}</a>
            			{% endfor %}
					</span>
					<button type="button" style="float:right" class="btn btn-default btn-sm"><span class="glyphicon glyphicon-star">{{ article.view_count }}</span></button>
					<button type="button" style="float:right" class="btn btn-default btn-sm"><span class="glyphicon glyphicon-comment">{{ article.comment_count }}</span></button>
				</div>
			</div>
			<div class="col-md-4 col-md-pull-8">
          		<img class="featurette-image img-responsive center-block" src="{{ article.imageURL}}" alt="Generic placeholder image">
        	</div>
		</div>
	{% endfor %}
{% paginate %}
{% endblock %}